{
  "name": "afkdev",
  "version": "0.0.1",
  "description": "Remote Desktop Environment",
  "scripts": {
    "buildClient": "cd client && npm install && npm run check",
    "buildServer": "cd server && npm install && npm run check",
    "dev": "npm run buildServer && npm run buildClient && cd server && npm run dev"
  },
  "author": "Gurdit Singh Siyan",
  "license": "MIT"
}
